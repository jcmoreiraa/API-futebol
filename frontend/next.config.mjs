/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['upload.wikimedia.org', 'example.com'], // Adicione outros domínios que você usar
    },
  };
export default nextConfig;
